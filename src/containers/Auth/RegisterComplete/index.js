import React from "react";
import { CRegisterComplete, PublicLayout } from "../../../components";

const RegisterComplete = () => {
  return (
    <PublicLayout>
      <CRegisterComplete />
    </PublicLayout>
  );
};

export default RegisterComplete;
